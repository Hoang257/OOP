# ЛР-7 — консольное приложение

Интерактивное CLI для коллекции спортсменов (модели **ЛР1–3**, коллекция и стратегии **ЛР5**). Код предметной области в `src/lib/lab01` … `lab05` **не изменяется**.

## Запуск

```bash
cd src/lib/lab07
python main.py
```

Или **F5** в VS Code (конфигурация «ЛР-7» в `.vscode/launch.json`), либо двойной щелчок по `run.bat`.

Данные: `data/athletes.json` (создаётся/обновляется при выходе, пункт **0**).

## Структура (слои)

| Файл | Назначение |
|------|------------|
| `main.py` | Точка входа, `sys.path` → корень `OOP` |
| `cli.py` | Меню, ввод/вывод, `try/except` |
| `app.py` | Бизнес-логика, коллекция, поиск, сортировка |
| `models.py` | Реэкспорт `Athlete`, `Runner`, `Swimmer` |
| `storage.py` | `save()` / `load()` — JSON |
| `exceptions.py` | `DuplicateAthleteError`, `AthleteNotFoundError` |

## Меню (по заданию)

1. Добавить (атлет / бегун / пловец)  
2. Показать всех (таблица)  
3. Найти по ID  
4. Найти по имени  
5. Поиск по атрибуту  
6. Фильтрация (активные, возраст, нагрузка, бегуны, пловцы)  
7. Сортировка: имя / нагрузка («цена») / дата добавления  
8. Изменить число посещений  
9. Удалить по ID (`y/n`)  
0. Выход + автосохранение  

## Соответствие ТЗ

- Разделение **CLI → app → models/storage**  
- Собственные исключения, перехват в CLI  
- Ввод пункта меню: `int(input(...))` + `except ValueError`  
- Оценка **5**: JSON, типизация в `app.py`, дата добавления в файле (не в классах)  

## Демонстрация работы

Одна запись **asciinema** должна показать все четыре сценария ниже (оценка на **5** — те же сценарии, что и для скриншотов).

### Сценарии (что показать в записи)

| № | Сценарий | Действия в CLI |
|---|----------|----------------|
| 1 | Старт и загрузка | Запуск `python main.py` → сообщение о файле данных → **2** (таблица; можно пустую) |
| 2 | CRUD и сохранение | **1** добавить бегуна и пловца → **2** → **9** удалить одного (`y`) → **0** → снова `python main.py` → **2** (данные сохранились) |
| 3 | Сортировка | **7** → по имени → **2**; снова **7** → по нагрузке → **2**; **7** → по дате добавления → **2** |
| 4 | Фильтр и ошибки | **6** (например, только бегуны) → **1** с тем же **ID** (дубликат) → **3** с несуществующим ID → в меню ввести `abc` (неверный пункт) |

Скриншоты (по желанию преподавателя): PNG в `images/lab07/` в корне репозитория.

### Звёздочка: asciinema (пошагово)

Документация: [docs.asciinema.org](https://docs.asciinema.org/)

#### 1. Установка (Windows)

**Вариант A — pip (проще всего):**

```powershell
pip install asciinema
```

**Вариант B — официальные способы:** [Installation](https://docs.asciinema.org/manual/cli/installation/) (Scoop, WSL и т.д.).

Проверка: `asciinema --version`

#### 2. Аккаунт

1. Зарегистрируйтесь на [asciinema.org](https://asciinema.org/).
2. Один раз привяжите CLI (токен в личном кабинете):

```powershell
asciinema auth
```

Вставьте API token, когда программа попросит.

#### 3. Запись

Откройте терминал в корне репозитория `OOP`:

```powershell
chcp 65001
cd C:\Users\hoang\OneDrive\Desktop\laba\OOP
asciinema rec lab07.cast
```

В **новой** оболочке внутри записи:

```powershell
cd src\lib\lab07
python main.py
```

Пройдите сценарии **1–4** из таблицы выше. Завершите программу пунктом **0**.

Остановить запись: **Ctrl+D** или введите `exit` и нажмите Enter.

#### 4. Публикация

После остановки asciinema спросит:

- **Upload?** → `y`
- **Title** → например: `ЛР-7 — CLI спортсмены`
- при необходимости — описание

В консоли появится ссылка вида `https://asciinema.org/a/1234567` — число `1234567` это **id** записи.

Локальный файл без загрузки: `asciinema upload lab07.cast`

#### 5. Ссылка в README

Замените `xxxxxxx` на свой id и вставьте в этот раздел:

[![asciinema](https://asciinema.org/a/xxxxxxx.svg)](https://asciinema.org/a/xxxxxxx)

**Пример** (подставьте свой id):

```markdown
[![asciinema](https://asciinema.org/a/1234567.svg)](https://asciinema.org/a/1234567)
```

#### 6. Проверка

- Ссылка открывается в браузере без входа.
- В записи видны все 4 сценария, кириллица читается (если каша — перезапишите после `chcp 65001`).
- Длительность обычно **3–7 минут** — не слишком коротко и не «пустой» скролл.

### Если asciinema не ставится

Запись через веб: [Getting started](https://docs.asciinema.org/getting-started/) — или установите [asciinema CLI в WSL](https://docs.asciinema.org/manual/cli/installation/) и запускайте `python main.py` из Linux-пути к проекту.
